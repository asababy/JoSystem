<UserControl x:Class="JoSystem.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JoSystem.Views"
             xmlns:vm="clr-namespace:JoSystem.ViewModels"
             xmlns:conv="clr-namespace:JoSystem.Converters"
             xmlns:jo="clr-namespace:JoSystem.Components.JoGrid"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}"
             FontFamily="{StaticResource GlobalFontFamily}">

    <UserControl.DataContext>
        <vm:SettingsViewModel />
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <conv:InvertBoolConverter x:Key="InvertBoolConverter"/>
    </UserControl.Resources>

    <Grid Margin="30">
        <!-- 只有一列，充满 -->
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="20" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,10,0"
                                   Background="Transparent"/>
                        <TextBlock Text="{DynamicResource Lang.Settings.General}" Style="{StaticResource HeaderTextStyle}" Margin="0"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{DynamicResource Lang.Settings.Language}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox ItemsSource="{Binding LanguageOptions}" 
                                  SelectedValue="{Binding SelectedLanguage}"
                                  DisplayMemberPath="Value"
                                  SelectedValuePath="Key"
                                  Width="140" Height="30" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- 视图缓存设置 -->
                <Expander Grid.Row="1" Margin="0,0,0,15" IsExpanded="False" BorderThickness="0" BorderBrush="#EEEEEE">
                    <Expander.Header>
                        <TextBlock Text="{DynamicResource Lang.Settings.ViewCaching}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </Expander.Header>
                    <ItemsControl ItemsSource="{Binding ViewCacheOptions}" Margin="15,0,15,15">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="5" HorizontalAlignment="Left"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding ViewName}" IsChecked="{Binding IsUnCached}" Margin="0,5,15,5" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>

                <jo:JoGrid Grid.Row="2" ItemsSource="{Binding Configs}">
                        <DataGridTextColumn Header="#" Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}" IsReadOnly="True" Width="40"/>
                        <DataGridTextColumn Binding="{Binding Key}" IsReadOnly="True" Width="200">
                            <DataGridTextColumn.Header>
                                <TextBlock Text="{DynamicResource Lang.Settings.ConfigKey}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Description}" IsReadOnly="True" Width="250" Foreground="{StaticResource TextSecondaryBrush}">
                            <DataGridTextColumn.Header>
                                <TextBlock Text="{DynamicResource Lang.Settings.Description}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.Header>
                                <TextBlock Text="{DynamicResource Lang.Settings.Value}"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ContentControl Content="{Binding}" VerticalAlignment="Center" Margin="5,0">
                                        <ContentControl.Style>
                                            <Style TargetType="ContentControl">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                                                     VerticalContentAlignment="Center" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="ToolTip" Value="{Binding Description}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Key}" Value="EnableHttps">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <ComboBox ItemsSource="{Binding DataContext.BooleanOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                              SelectedValue="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              SelectedValuePath="Value"
                                                                              DisplayMemberPath="Key"
                                                                              VerticalContentAlignment="Center" />
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Key}" Value="RunAsService">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <ComboBox ItemsSource="{Binding DataContext.BooleanOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                              SelectedValue="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              SelectedValuePath="Value"
                                                                              DisplayMemberPath="Key"
                                                                              IsEnabled="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                              VerticalContentAlignment="Center" 
                                                                              ToolTip="{DynamicResource Lang.Config.RunAsServiceTooltip}"/>
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Key}" Value="EnableSwagger">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <ComboBox ItemsSource="{Binding DataContext.BooleanOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                              SelectedValue="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                              SelectedValuePath="Value"
                                                                              DisplayMemberPath="Key"
                                                                              VerticalContentAlignment="Center" />
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Key}" Value="UnCachedViews">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <TextBox Text="{Binding Value, Mode=OneWay}" 
                                                                             IsReadOnly="True" 
                                                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                                                             VerticalContentAlignment="Center" />
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ContentControl.Style>
                                    </ContentControl>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                </jo:JoGrid>

                <Grid Grid.Row="3" Margin="0,20,0,0">
                    <Button Command="{Binding ResetToDefaultsCommand}" HorizontalAlignment="Left" Style="{StaticResource SecondaryButtonStyle}" Width="120" Height="{StaticResource StandardControlHeight}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE777;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{DynamicResource Lang.Settings.RestoreDefaults}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding ReloadConfigCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,12,0" Height="{StaticResource StandardControlHeight}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource Lang.Settings.Reload}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding SaveConfigCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="120" Height="{StaticResource StandardControlHeight}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource Lang.Settings.Save}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
